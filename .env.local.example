# factcheck-agent 환경변수
# 이 파일을 .env.local 로 복사 후 값 채우기. .env.local 은 절대 커밋 금지.

# Anthropic API 키 (필수). 팩트체크 검색·정리에 사용.
ANTHROPIC_API_KEY=

# Upstash Redis (필수). 결과 저장 + 공유 링크 + 최근 목록.
# 미설정 시 메모리 캐시로 폴백 (서버 재시작 시 사라짐, 로컬 개발용).
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
